import { Link } from "react-router-dom";

const HeaderVinted = () => {
  return (
    <Link to="/login">
      <button type="button">connexion</button>
    </Link>
  );
};
export default HeaderVinted;
